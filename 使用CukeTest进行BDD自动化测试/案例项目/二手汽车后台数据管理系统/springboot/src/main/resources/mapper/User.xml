<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <resultMap id="userMap" type="com.example.demo.entity.User">
        <result property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickName" column="nick_name"/>
        <result property="age" column="age"/>
        <result property="sex" column="sex"/>
        <result property="address" column="address"/>
        <result property="role" column="role"/>
        <collection property="usedcarList" javaType="ArrayList" ofType="com.example.demo.entity.Usedcar">
            <result column="c_id" property="id" />
            <result column="c_name" property="name" />
            <result column="c_price" property="price" />
        </collection>
    </resultMap>

    <select id="findPage" resultMap="userMap">
        SELECT `user`.* ,usedcar.id as c_id, usedcar.name c_name,usedcar.price c_price from `user`
        left join usedcar on user.id = usedcar.user_id where `user`.nick_name like concat('%', #{nickName}, '%')
    </select>
</mapper>
