<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UsedcarMapper">
    <select id="findByUserId" resultType="com.example.demo.entity.Usedcar">
        select `usedcar`.*, `user`.nick_name
        from `usedcar`
        left join `user` on `usedcar`user_id = `user`.id
        where `usedcar`.user_id = #{userId}
    </select>

</mapper>